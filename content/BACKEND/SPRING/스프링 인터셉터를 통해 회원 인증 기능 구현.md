---
created: 2026-02-25T22:29:00+09:00
notion-id: 312737bad00c80818cefd7dcb319c955
---
기존에 `@SessionAttribute` 방법은 모든 컨트롤러에 적용해야 하는데 그러면 같은 코드를 모든 컨트롤러에서 반복해야 하고, 인증 로직이 여기저기 흩어져서 유지보수가 힘들어진다.

또한 인증은 공통 관심사인데, 컨트롤러가 그 책임까지 떠안게 되기 때문에 코드가 무거워진다. 그래서 스프링 인터셉터를 통해서 인증 로직을 한 곳으로 모으는 방법이 좋다.

```java
@Slf4j
public class LoginCheckInterceptor implements HandlerInterceptor {
    @Override
    public boolean preHandle(HttpServletRequest request, HttpServletResponse response, Object handler) throws Exception {

        String requestURI = request.getRequestURI();
        log.info("인증 체크 인터셉터 실행 {}", requestURI);

        HttpSession session = request.getSession();
        if (session == null || session.getAttribute(SessionConst.LOGIN_MEMBER) == null) {
            log.info("미인증 사용자 요청");

            // 로그인으로 리다이렉트 + return false로 컨트롤러 호출 막기
            response.sendRedirect("/login?redirectURL=" + requestURI);
            return false;
        }
        return true;
    }
}
```

- `preHandle`에 작성 → 컨트롤러에 도달하는 모든 요청에 대해 사전 검사를 수행한다.
	- 물론 설정 파일에서 인터셉터에 등록된 URL에서만 해당 인터셉터가 작동함.
- 컨트롤러에서 `@SessionAttribute`를 통해서 인증 과정을 거칠 필요가 사라진다.
	- 세션 값이 실제로 필요한 경우에만 `@SessionAttribute`로 주입을 받으면 된다.
- 페이지 입장 전의 회원 여부와 같은 공통 관심사는 스프링 인터셉터 기능을 활용하자.

```java
@Configuration
public class WebConfig implements WebMvcConfigurer {

    @Override
    public void addInterceptors(InterceptorRegistry registry) {
        registry.addInterceptor(new LoginCheckInterceptor())
                .order(1)
                .addPathPatterns("/**")
                .excludePathPatterns("/", "/members/add", "/login", "logout", "/css/**", "/*.ico", "/error");
    }
}
```

- 물론 `WebMvcConfigurer`와 같은 설정 파일에 인터셉터를 등록해야 작동한다

### 참고

[[HandlerInterceptor]]

[[@SessionAttribute]]

[[WebMvcConfigurer]]