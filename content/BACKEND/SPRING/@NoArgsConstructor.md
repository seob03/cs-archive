---
notion-id: 313737bad00c80fe9d72da2497d3f5b5
---
`@NoArgsConstructor`는 **“인자 없는 생성자(기본 생성자)”를 Lombok이 자동으로 만들어주는 애노테이션**이다. (JPA 엔티티에서 사실상 필수)

**프레임워크가 객체를 “빈 껍데기”로 먼저 만들 수 있게 해주는 기본 생성자 자동 생성한다.**(특히 JPA에서 핵심)